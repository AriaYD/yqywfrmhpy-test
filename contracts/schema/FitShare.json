{
  "$defs": {
    "FitTag": {
      "description": "区分\"活动不好\"与\"对我不合适\"（Spec §17.4 Personal-vs-Global Separation）。",
      "enum": [
        "too_basic_for_me",
        "too_advanced_for_me",
        "wrong_format_for_me",
        "schedule_mismatch",
        "good_fit"
      ],
      "title": "FitTag",
      "type": "string"
    }
  },
  "$id": "https://campuspath.invalid/schema/FitShare.json",
  "additionalProperties": false,
  "description": "契合标签在已验证反馈中的占比（2026-08-04 用户裁定上呈现层）。\n\n契合是**个人判断**（§17.4 Personal-vs-Global Separation），不折进\n质量分——但它的**分布**是给主办方的有效信号（\"40% 说太基础\" =\n受众标注要改）。以独立字段出域、前端独立分区展示，隔离不破。",
  "properties": {
    "fit": {
      "$ref": "#/components/schemas/FitTag"
    },
    "share": {
      "maximum": 1,
      "minimum": 0,
      "title": "Share",
      "type": "number"
    }
  },
  "required": [
    "fit",
    "share"
  ],
  "title": "FitShare",
  "type": "object"
}
