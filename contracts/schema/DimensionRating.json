{
  "$defs": {
    "QualityDimension": {
      "description": "固定维度。新增维度要改 Spec，不能由前端随手加一个自由文本问题。",
      "enum": [
        "content_depth",
        "organization",
        "practical_value",
        "networking_value",
        "expectation_match"
      ],
      "title": "QualityDimension",
      "type": "string"
    }
  },
  "$id": "https://campuspath.invalid/schema/DimensionRating.json",
  "additionalProperties": false,
  "properties": {
    "dimension": {
      "$ref": "#/components/schemas/QualityDimension"
    },
    "rating": {
      "maximum": 5,
      "minimum": 1,
      "title": "Rating",
      "type": "integer"
    }
  },
  "required": [
    "dimension",
    "rating"
  ],
  "title": "DimensionRating",
  "type": "object"
}
